// Import React components from the React library build
const ReactComponents = require('./react-components/src/index');

// Import Angular components from the Angular library build
const AngularComponentsComponent = require('./angular-components/index');

// Export the components
module.exports = {
  ReactComponents,
  AngularComponentsComponent
};
